# apt manager

each script located at `$LIBRARY/packages/${package}/apt.sh` simple defines the following
environmental variables:

- (required) `APT_PACKAGES`: array of ids of the target apt packages
